/**
 * DEPRECATED — replaced by lightweight CSS visuals in the 2D refactor.
 * Kept as a no-op stub to avoid breaking any stale imports. Not rendered.
 */
export default function GeometricViz({ className = "" }: { className?: string }) {
  return <div className={className} aria-hidden />;
}
